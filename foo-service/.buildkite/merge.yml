steps:
  - label: "Deploy Foo service to staging"
    command:
      - "echo linting"
      - "echo testing"
      - "echo deploying-to-staging"

  - wait

  - block: ":rocket: Release to Production"

  - label: "Deploy Foo service to production"
    command:
      - "echo deploying-to-production"
